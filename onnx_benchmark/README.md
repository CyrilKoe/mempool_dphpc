# ONNX Benchmarks for Reduction

- To sweep over input size, use `script_inputsize.sh`.
- To sweep over number of threads, use `script_nthreads.sh`.

Output traces can be analyzed using `analyze_*.py`.
